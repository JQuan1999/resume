# 04 · 综合、软性问题与反问

---

## 一、定位类（每场必问，决定第一印象）

### Q1. 你为什么想做大模型推理？

⚠️ **不要说"因为大模型是风口"、"因为有前景"**。这是最差的答案。
好的答案要体现**连续性**——你不是追热点，是原有能力的自然延伸。

> 我的路径其实是连续的。我入职后第一年做的通用推理引擎，
> 本身就是推理方向的工作——只是当时是小模型，TensorRT + 动态 batch + 插件化，
> 支撑了部门 200 多个模型。
>
> 后来业务开始上大模型，我很自然地接了过来：先是部署侧，
> 把 vLLM、SGLang、TRT-LLM 三套后端封成统一镜像和平台；
> 再往下就必然要碰引擎内部——比如做约束解码时，
> 我发现光会用框架是不够的，PyTorch backend 的 logits processor 只给 beam0
> 这种限制，不读源码根本不知道。
>
> 所以我想做推理，不是因为它热，是因为我已经在这条路上了，
> 而且我发现自己真正感兴趣的是**下沉到引擎内部**这部分——
> 我自学的方向也都在这里：读完了 nano-vLLM 的全部实现、写 Triton kernel、
> 跟 FlashAttention 的代码。我希望下一份工作能让我从"用框架和搭平台"
> 走到"改框架和做优化"。

**这个答案的三个关键点**：① 有连续性不是跳槽追风口；② 有具体的技术细节佐证；
③ 明确说出想要什么（从用框架到改框架），这让面试官知道怎么安排你。

---

### Q2. 你目前的工作里，推理占多少？平台和运维占多少？

**这题是在戳你简历的软肋，必须提前准备。别回避。**

> 坦白说，过去一年平台和交付的比重不小——多后端镜像、上线平台、私有化部署，
> 这些都是工程交付性质的工作。
>
> 但有两块是实打实的推理工程：一是自研的 C++ 推理引擎，
> 动态 batch 的凑批策略、多 runtime 抽象、TRT 7 到 10 的升级和 200+ 模型的精度验证，
> 都是我做的；二是约束解码那个项目，从框架选型到 profiling 定位 D2H 同步瓶颈再到重构，
> 是纯粹的推理优化工作。
>
> 我很清楚自己的短板在**算子层和 kernel 级优化**——
> 这块我目前是自学水平，写过 Triton 的练习 kernel，读过 FlashAttention 的实现，
> 但没在生产里写过算子。这正是我换工作想补上的部分。

⚠️ **主动承认短板 + 说清楚想补什么**，这比假装全面强得多。
面试官最怕的不是能力有缺口，是候选人不知道自己的缺口在哪。

---

### Q3. 你只有 2 年经验，凭什么做推理？

> 两年里我接触的推理场景可能比一般两年的人杂一些：
> 小模型有 TensorRT 和 ONNX Runtime，大模型有 vLLM、SGLang、TRT-LLM，
> 硬件上除了英伟达还做过昇腾，部署形态上有云上 K8s 也有 ToB 私有化交付。
> 广度是有的。
>
> 深度上我承认还在爬坡，所以我用自学在补：
> nano-vLLM 我不是读一遍，是把 BlockManager 打了插桩跑各种边界场景，
> 确认我对前缀缓存和块分配的理解和代码一致；
> Triton 我从 matmul 开始手写，理解 tile 和访存的关系。
>
> 我的判断是：推理这个方向，**有系统工程底子 + 肯往下读源码**是能快速起来的，
> 而我的 C++ 和线上问题排查能力是实战练出来的，这部分不需要再补。

---

## 二、项目类

### Q4. 讲一个你最有成就感的项目 / 最难的技术问题

**推荐用 GR 那个 D2H 优化**，理由：它是纯技术的、可量化的、有清晰的"发现-归因-解决"链条。
用 STAR 讲：

> **S**：做生成式检索的在线推理，要求 Trie 约束解码 + beam search，
> 且打分口径要和离线完全一致。
> **T**：第一版跑通后延迟远超业务预算，需要找到瓶颈。
> **A**：profiling 发现 GPU 计算时间远小于墙钟时间，中间是等待。
> 顺着看下去定位到 logits processor——每个 decode step 把 15 万维词表 logits 拷回 CPU，
> masking 前后各一次。关键认知是：**问题不是拷贝的字节数，是 D2H 构成强制流同步**，
> 让本来流水线化的 decode 变成每步都排空一次。
> 改法是把整段计算移进 GPU stream，中间结果显存驻留，
> 只在生成结束时 gather 取回两个标量。
> **R**：每请求设备同步次数从 O(生成长度) 降到 1 次，P99 降低【补】%。
> 但整体仍不达标，我把剩余瓶颈（Python 层每步的 mask 构建和索引 H2D）也定位清楚了，
> 出了评估报告，这个方案最终没有继续推。

**结尾一定要加这句**：
> 这个项目最后没上线，但我觉得它是我这两年收获最大的一个——
> 它逼着我从"调框架参数"下沉到"看框架怎么执行的"。

---

### Q5. 有没有失败的项目？你怎么看？

**你正好有一个现成的、且已经在简历上写明的，直接用。** 这题不要临场编。

> 就是刚才那个约束解码。我投入了一段时间，最后结论是方案不可行。
>
> 我的复盘是两点：
> **第一是可行性验证应该更早。** 我是把功能做完、精度对齐做完才开始压性能的，
> 其实在选型阶段就应该先拿一个最小原型测延迟上限——
> beam=5 加上每步全词表的掩码操作，这个开销量级是可以提前估出来的。
> 先测天花板再做功能，能省掉很多。
> **第二是结论要有数据。** 我最后交的不是"做不了"，
> 是一份带 profiling 数据、说明瓶颈在哪、以及要做到需要付出什么的报告。
> 这样团队才好判断是彻底放弃还是换方向。

**这个回答的价值在于**：它展示了工程判断力和复盘能力，
而且"先验证天花板再做功能"是一条真正的经验教训，不是套话。

---

### Q6. 你的缺点是什么？

⚠️ 不要说"我太追求完美"这种假缺点。说真的，但要配上正在做的改进。

> 一个是刚说的——**验证顺序上容易先做功能再验性能**，
> 这个我在 GR 那个项目上付了学费，现在会先测可行性边界。
>
> 另一个是**底层优化的经验不足**。我的强项在系统工程和框架层，
> 但 CUDA kernel、算子融合这些我目前只到看得懂、写得了练习代码的程度，
> 没有生产经验。我在用业余时间补，但这确实是我现在最大的能力缺口。

---

## 三、动机与规划

### Q7. 为什么考虑换工作？

**基调：追求方向的深入，不要抱怨现公司。**

> 主要是方向。我现在的岗位覆盖面很广，推理、平台、私有化交付都做，
> 这让我快速建立了全局认知，但也意味着每块都不够深。
> 我想找一个能专注在推理引擎本身的位置——
> 能持续做性能优化、能碰到引擎内部，而不是在框架外面做集成和交付。
>
> 另外我们的大模型推理更多是"跟进和使用"上游框架，
> 我希望下一步能参与到框架内部的改造和优化里去。

⚠️ **千万别说"现在的活太杂太累"、"领导不重视"**。说方向，不说人和事。

### Q8. 你未来 3 年的规划？

> 短期一年，先把 kernel 和算子这块的缺口补上，
> 能独立做端到端的性能分析和优化——从 profiling 定位到具体的优化落地。
> 中期，希望能深入到调度和显存管理这一层，
> 比如 batching 策略、KV Cache 管理、投机解码这些方向做出实际的性能收益。
> 长期，我希望成为能对推理性能整体负责的人——
> 给定模型和硬件，能说清楚天花板在哪、当前差多少、怎么补。
>
> 我不太想做纯管理，至少这几年想在技术上扎下去。

---

## 四、反问环节（一定要问，且要问得有水平）

**反问是双向的**：既是你评估这份工作，也是面试官评估你的最后一个窗口。
准备 5 个，现场按聊天内容挑 2-3 个问。

### 强烈推荐（显示你真的懂这行）

1. **团队现在推理这块的主要瓶颈是什么？是吞吐、延迟、还是成本？**
   → 这个问题问出来就说明你知道推理优化是分目标的。

2. **你们是基于开源框架二次开发，还是有自研的推理引擎？
   如果是二次开发，改动主要集中在哪一层——调度、显存、还是算子？**
   → 直接问到你最关心的"能不能碰引擎内部"。

3. **这个岗位进来后，前三个月最希望解决的一个问题是什么？**
   → 问出真实的工作内容，避免 JD 和实际不符。

4. **团队怎么衡量推理优化的收益？有没有固定的 benchmark 和回归流程？**
   → 显示你在意工程化和可度量性，不是拍脑袋调参。

### 可选

5. 团队的技术分享和代码 review 是怎么做的？
6. 目前团队的规模和构成？做引擎的和做平台的比例大概是多少？
7. 你们对国产芯片（昇腾/寒武纪）有布局吗？

### 不要问

- ❌ 加班多不多、能不能 WLB（放到 HR 面）
- ❌ 薪资（技术面不谈钱）
- ❌ "贵公司的企业文化是什么"（空洞，浪费机会）
- ❌ 任何能从官网查到的信息

---

## 五、开场自我介绍（2 分钟，背熟）

> 我叫蒋权，2024 年硕士毕业于西电计算机，之后一直在 360 搜索部门做服务端开发，
> 到现在两年出头。
>
> 我的工作主线是推理。第一年做部门的通用推理引擎——C++17 自研，
> TensorRT 和 ONNX Runtime 双后端，我负责动态 batch 的凑批调度、
> 插件化的前后处理体系，以及框架从 TensorRT 7 到 10 的整体升级，
> 这套框架现在支撑了 200 多个小模型在线，做资源优化时累计省下了 200 多张 T4。
>
> 第二年重心转到大模型推理。做了三件事：一是把 vLLM、SGLang、TensorRT-LLM
> 三套后端封装成统一镜像和统一代理，屏蔽后端差异；
> 二是主导了大模型上线平台的建设，打通模型管理、插件管理到应用发布的链路；
> 三是做了一些定制推理开发，比如在 TRT-LLM 的 C++ backend 上实现 Trie 约束的
> beam search 解码，过程中定位并消除了解码热路径上的 D2H 同步瓶颈。
>
> 另外我还负责了安全大模型在昇腾和英伟达双芯片上的私有化部署，
> 包括模型加密和设备鉴权这一套。
>
> 自学这块，我精读了 nano-vLLM 的全部实现，写过 Triton kernel，
> 一直在跟 vLLM 和 TRT-LLM 的上游代码。
>
> 我希望下一步能更专注在推理引擎本身，做更底层的性能优化工作。

**时长控制在 100-120 秒。** 结构是：基本信息 → 主线（第一年/第二年）→
自学 → 求职意图。最后一句是钩子，引导面试官往推理方向问。

⚠️ **自我介绍里不要提风控干预系统**，会把话题带偏到业务后端。
被问到再说。
