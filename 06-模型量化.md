# 06 · 模型量化（从零到能上线）

> 这份是**学习向**文档，假设你对量化零基础，从数值表示讲起，一路到 vLLM 上线。
> 和 [01-大模型推理.md](01-大模型推理.md) 的 Q0 / Q10 的关系：
> 那边是**面试怎么答**（浓缩、口播稿），这边是**你自己怎么理解和动手**（展开、有推导）。
> 先读这份建立体系，再回去背那边的答案。

---

## 目录

1. [为什么需要量化](#一为什么需要量化)
2. [前置知识：计算机怎么存小数](#二前置知识计算机怎么存小数)
3. [量化的数学：一个乘法和一个四舍五入](#三量化的数学一个乘法和一个四舍五入)
4. [误差从哪来：手算一个例子](#四误差从哪来手算一个例子)
5. [量化的三个独立维度](#五量化的三个独立维度)
6. [粒度：scale 共享的范围](#六粒度scale-共享的范围)
7. [为什么激活比权重难量化](#七为什么激活比权重难量化)
8. [主流算法详解](#八主流算法详解)
9. [算法 / 格式 / Kernel：三件不同的事](#九算法--格式--kernel三件不同的事)
10. [硬件支持矩阵](#十硬件支持矩阵)
11. [在 vLLM 上实操](#十一在-vllm-上实操)
12. [怎么验证量化做对了](#十二怎么验证量化做对了)
13. [高频坑](#十三高频坑)
14. [决策表与术语表](#十四决策表与术语表)

---

## 一、为什么需要量化

### 1.1 先看一笔账

一个 7B 模型，权重用 FP16（每个参数 2 字节）存：

```
7,000,000,000 × 2 字节 ≈ 14 GB
```

一张 T4 只有 16GB 显存。14GB 放权重，剩下 2GB 要装 KV Cache、激活值、CUDA context——**基本跑不动，或者只能跑极短的上下文、极低的并发**。

换成 INT4（每个参数 0.5 字节）：

```
7,000,000,000 × 0.5 字节 ≈ 3.5 GB
```

省下来的 10GB 全部可以给 KV Cache，**并发数能翻好几倍**。这是量化最直观的收益：**省显存**。

### 1.2 但真正的大头是"省带宽"

这一节是理解量化的关键，慢慢看。

LLM 推理分两个阶段：

| 阶段 | 在做什么 | 计算量 | 权重读取量 |
|---|---|---|---|
| **Prefill** | 把整个 prompt（比如 512 token）一次性过一遍模型 | 大（512 个 token 的矩阵乘） | 整个模型读 1 遍 |
| **Decode** | 每次只生成 1 个 token | **极小**（1 个 token） | 整个模型**还是**读 1 遍 |

注意 decode 那一行：**为了算出 1 个 token，你要把 14GB 的权重从显存搬到计算单元一次。**

T4 的显存带宽约 320 GB/s，所以光是搬权重就要：

```
14 GB / 320 GB/s ≈ 44 ms
```

而这 44ms 里，GPU 的计算单元（Tensor Core）几乎是闲着的——batch=1 时那点矩阵乘，算力占用可能不到 5%。

> **这种"瓶颈在搬数据、不在算数据"的状态，叫 memory-bound（访存受限）。**
> 反之叫 compute-bound（计算受限）。

所以：

> ### ⭐ 核心结论
> **decode 阶段是 memory-bound 的，瓶颈是权重读取量。把权重从 16bit 压到 4bit，
> 读取量降到 1/4，decode 速度就近似提升到 4 倍。**

这就是为什么量化在 LLM 上的收益远远大于在传统 CNN 上——CNN 通常是 compute-bound 的，压缩权重帮助有限。

### 1.3 立刻记住这条反面

> ### ⚠️ 量化不等于一定变快
> **大 batch 的 prefill 阶段是 compute-bound 的**——这时候权重只读一遍但要算几千个 token
> 的矩阵乘，瓶颈在算力不在带宽。此时 W4A16（只压权重、算的时候还要反量化回 FP16）
> **不但没有收益，反量化还要额外开销，可能更慢。**

这条是初学者最容易踩的坑，也是面试的高频追问。**判断的依据永远是：你的 workload 是 prefill 重还是 decode 重。**

粗略判断法：

```
输入长、输出短（如文本分类、Query 改写）  → prefill 重  → weight-only 量化收益小
输入短、输出长（如写作、对话、CoT 推理）  → decode 重   → weight-only 量化收益大
```

### 1.4 顺便说清楚：量化 ≠ 剪枝 ≠ 蒸馏

| 手段 | 做了什么 | 模型结构变了吗 |
|---|---|---|
| **量化** | 每个数用更少的比特表示 | 不变，参数个数不变 |
| **剪枝** | 删掉不重要的权重/整个 head/整层 | 变了 |
| **蒸馏** | 用大模型教一个小模型 | 完全是另一个模型 |

量化是三者里**最安全、最工程化、性价比最高**的，所以生产上用得最多。

---

## 二、前置知识：计算机怎么存小数

要理解量化误差，得先知道原来的数是怎么存的。

### 2.1 浮点数的三段结构

一个浮点数 = `符号位(sign) + 指数位(exponent) + 尾数位(mantissa)`

```
数值 ≈ (-1)^sign × 2^exponent × 1.mantissa
        ↑符号      ↑决定"能表示多大范围"   ↑决定"精度有多细"
```

| 类型 | 总位数 | 符号 | 指数 | 尾数 | 能表示的最大值 | 特点 |
|---|---|---|---|---|---|---|
| FP32 | 32 | 1 | 8 | 23 | ~3.4e38 | 训练的基准精度 |
| **FP16** | 16 | 1 | 5 | 10 | 65504 | **推理最常用**，范围偏窄易溢出 |
| **BF16** | 16 | 1 | 8 | 7 | ~3.4e38 | 范围和 FP32 一样宽、精度差一点，**训练常用** |
| **FP8 E4M3** | 8 | 1 | 4 | 3 | 448 | 前向推理用这个 |
| FP8 E5M2 | 8 | 1 | 5 | 2 | 57344 | 范围更宽精度更差，梯度用 |

> **BF16 vs FP16 的一句话区别**：BF16 牺牲精度换范围，所以训练时不容易溢出（梯度可能非常小或非常大）；FP16 精度高但范围窄。推理阶段数值范围可控，所以 FP16 更常见。

### 2.2 整数：均匀但呆板

| 类型 | 位数 | 可表示的值 | 有多少个格子 |
|---|---|---|---|
| INT8 | 8 | -128 ~ 127 | 256 |
| INT4 | 4 | -8 ~ 7 | 16 |

### 2.3 最重要的一张图：格子的疏密

这是 FP8 和 INT8 的本质差别，**理解了这个就理解了为什么 FP8 精度更好**。

```
浮点数（FP8）的可表示值分布——靠近 0 极密，远离 0 极疏：
   ||||||||| | | |  |   |    |      |         |
   0                                         448

整数（INT8）的可表示值分布——完全均匀：
   | | | | | | | | | | | | | | | | | | | | | |
   0                                         127
```

而神经网络的权重分布**长什么样**？大致是个以 0 为中心的钟形（高斯）分布：绝大多数权重非常接近 0，极少数很大。

```
权重的实际分布：
            ▁▂▄██▄▂▁
        ────────┼────────
                0
```

**浮点数的格子分布，天然贴合权重的分布**——数据密集的地方（0 附近）格子也密集。整数则是均匀撒网，在 0 附近浪费不了、在远处又太密。

> ### ⭐ 这就是为什么同样 8 bit，FP8 的精度通常明显优于 INT8。
> 代价是需要硬件原生支持 FP8 运算——**Ada（L40S）和 Hopper（H100）及以后才有，
> T4 / A10 / A100 都没有。**

---

## 三、量化的数学：一个乘法和一个四舍五入

### 3.1 对称量化（权重的默认做法）

目标：把一组 FP16 的数，映射到 INT8 的 -127~127 上。

**第一步，算缩放因子 scale**——用这组数里绝对值最大的那个，去对齐整数的上限：

```
scale = max(|X|) / 127
```

**第二步，量化**（除以 scale 再四舍五入）：

```
X_int = clamp(round(X / scale), -127, 127)
```

**第三步，反量化**（用的时候再乘回去）：

```
X_fp16 ≈ X_int × scale
```

存到磁盘上的是 `X_int`（1 字节）+ 一个 `scale`（2 字节，但一组数共享）。这就是压缩的来源。

### 3.2 非对称量化（激活值常用）

如果这组数不是以 0 为中心的（比如经过 ReLU 之后全是非负数），对称量化会浪费掉一半的格子。这时用非对称：

```
scale = (max(X) - min(X)) / 255
zero_point = round(-min(X) / scale)        # 0 映射到哪个整数上

X_int  = clamp(round(X / scale) + zero_point, 0, 255)
X_fp16 ≈ (X_int - zero_point) × scale
```

多存一个 `zero_point`，换来对偏斜分布更好的利用率。

> **经验法则**：权重大致对称分布 → 用对称量化；激活值常常偏斜 → 用非对称量化。

### 3.3 静态 vs 动态

| | 什么时候算 scale | 用在哪 |
|---|---|---|
| **静态量化** | **离线**，用校准数据提前算好存下来 | 权重（权重是固定的，必须静态）；激活也可以静态 |
| **动态量化** | **运行时现算**，每个 batch/token 当场求 max | 只用于激活 |

动态量化精度更好（每次都用当前真实的范围），但运行时要多一步求 max 的开销。

> **FP8 dynamic 方案之所以不需要校准数据**，就是因为它权重静态 per-channel、激活动态 per-token——激活的 scale 运行时现算，压根不需要提前看数据。

---

## 四、误差从哪来：手算一个例子

**这一节请务必跟着算一遍，算完你对量化的直觉就建立起来了。**

假设某一组权重是：

```
X = [0.10, -0.20, 0.15, 0.30, -0.05, 0.25, -0.10, 12.0]
                                                  ↑ 离群值 outlier
```

### 4.1 INT8 对称量化

```
scale = 12.0 / 127 = 0.09449
```

| 原值 | X/scale | 四舍五入 | 反量化回来 | 相对误差 |
|---|---|---|---|---|
| 0.10 | 1.058 | 1 | 0.0945 | 5.5% |
| -0.20 | -2.117 | -2 | -0.1890 | 5.5% |
| 0.15 | 1.588 | 2 | 0.1890 | **26%** |
| 0.30 | 3.175 | 3 | 0.2835 | 5.5% |
| **-0.05** | -0.529 | **-1** | **-0.0945** | **89%** 😱 |
| 0.25 | 2.646 | 3 | 0.2835 | 13% |
| -0.10 | -1.058 | -1 | -0.0945 | 5.5% |
| 12.0 | 127 | 127 | 12.0 | 0% |

**看出问题了吗？** 那个 12.0 把 scale 撑大了，导致所有小数值只能落在极少数几个格子上，误差飙到 26%、89%。

如果把离群值拿掉，`scale = 0.30/127 = 0.00236`，`-0.05` 量化后是 `-0.0496`，误差只有 **0.8%**——**差了 100 倍。**

### 4.2 INT4 对称量化（只有 16 个格子）

```
scale = 12.0 / 7 = 1.714
```

| 原值 | X/scale | 四舍五入 | 反量化 |
|---|---|---|---|
| 0.10 | 0.058 | **0** | 0.0 |
| -0.20 | -0.117 | **0** | 0.0 |
| 0.15 | 0.088 | **0** | 0.0 |
| 0.30 | 0.175 | **0** | 0.0 |
| -0.05 | -0.029 | **0** | 0.0 |
| 0.25 | 0.146 | **0** | 0.0 |
| -0.10 | -0.058 | **0** | 0.0 |
| 12.0 | 7 | 7 | 12.0 |

**除了那个离群值，全军覆没，整组权重变成 0。** 模型直接废掉。

> ### ⭐ 这就是量化的核心矛盾
> **比特越少，格子越少，对离群值越敏感。**
> 后面要讲的所有算法（AWQ、GPTQ、SmoothQuant、分组量化），
> **本质上都在解决同一个问题：别让离群值毁掉整组数。**

### 4.3 误差的两个来源

```
总误差 = 舍入误差(rounding) + 截断误差(clipping)
```

- **舍入误差**：round 带来的，上限是 `scale/2`。scale 越小，舍入误差越小。
- **截断误差**：如果你为了缩小 scale，故意把离群值 clamp 掉（比如只取 99.9% 分位数当 max），离群值本身就失真了。

这两者是**跷跷板**：scale 调小→舍入误差小但截断误差大；scale 调大→反之。所有"求最优 scale"的算法，都是在这条曲线上找最低点。

---

## 五、量化的三个独立维度

初学者最常见的困惑是把量化当成一件事。实际上有**三个互相独立的开关**，可以任意组合：

```
┌─────────────┐   ┌─────────────┐   ┌─────────────┐
│  ① 权重量化  │   │ ② 激活量化   │   │ ③ KV Cache  │
│             │   │             │   │    量化      │
│ 省显存+带宽  │   │ 省算力       │   │ 省 KV 显存   │
│ 容易         │   │ 难           │   │ 中等         │
└─────────────┘   └─────────────┘   └─────────────┘
```

| 维度 | 收益 | 难度 | 为什么 |
|---|---|---|---|
| **① 权重** | 模型体积变小、decode 变快 | **低** | 权重是固定的，可以离线慢慢算最优 scale；分布规整 |
| **② 激活** | 矩阵乘能真正跑在 INT8/FP8 Tensor Core 上，**prefill 也变快** | **高** | 激活是运行时才产生的，且**有严重的离群值** |
| **③ KV Cache** | KV 显存减半 → **并发数近似翻倍** | 中 | K 比 V 敏感 |

### 5.1 WxAy 命名法怎么读

这是你会到处看到的写法：

| 写法 | 含义 | 算力有提升吗 | 适合什么 |
|---|---|---|---|
| **W4A16** | 权重 4bit，激活 16bit | **没有**。算之前要把权重反量化回 FP16 | decode 重、batch 小的场景 |
| **W8A8** | 权重 8bit，激活 8bit | **有**。矩阵乘直接在 INT8/FP8 Tensor Core 上跑 | prefill 重、大 batch 高吞吐 |
| W4A8 / W4A4 | 更激进 | 有 | 需要特定硬件（W4A4 要 Blackwell） |

> **W4A16 为什么还能加速？** 再回顾 1.2 节：decode 阶段瓶颈在**搬权重**，不在算。
> 权重小了 4 倍，搬得快 4 倍，即使算之前要多一步反量化（这一步很便宜），整体还是快。

### 5.2 KV Cache 量化是完全独立的一档

它和权重/激活量化互不干扰，可以叠加。为什么它重要：

```
KV Cache 每 token 占用 = 2(K和V) × 层数 × KV头数 × head_dim × 每个数的字节数
```

长上下文 + 高并发时，**KV Cache 可能比模型权重还占显存**。压到 FP8，每 token 占用减半，能同时服务的请求数近似翻倍。

---

## 六、粒度：scale 共享的范围

第四节已经看到：**一个 scale 管的数越多，越容易被离群值毁掉。** 所以就有了粒度的选择。

| 粒度 | 一个 scale 管多少数 | 额外存储 | 抗离群值 |
|---|---|---|---|
| **per-tensor** | 整个权重矩阵（几百万个数） | 可忽略 | 最差 |
| **per-channel** | 每个输出通道（一行/一列） | 很小 | 好 |
| **per-group** | 每 **128** 个元素一组（最常用） | 约 +2~3% 体积 | 最好 |
| per-token（激活用） | 每个 token 的向量 | 运行时算，不存 | 好 |

用第四节的例子说明 per-group 的作用：把 8 个数切成两组，每组 4 个，各用各的 scale。

```
G1 = [0.10, -0.20, 0.15, 0.30]   → scale = 0.30/7 = 0.0429  → 误差 7~14%，能用
G2 = [-0.05, 0.25, -0.10, 12.0]  → scale = 12.0/7 = 1.714   → 这一组还是废了
```

**分组不能消灭离群值，但能把损害关在一个小房间里**，不让它污染整个矩阵。这就是 `group_size=128` 成为行业默认值的原因——**它是"存储开销"和"抗离群值"的平衡点**。

> **实践默认配置（记住这一条就够）**：
> 权重 per-group(128) 或 per-channel，激活 per-token 动态量化。

---

## 七、为什么激活比权重难量化

这是理解 AWQ / SmoothQuant 的前提。

### 7.1 现象

研究者发现：LLM 的激活值里，**极少数几个 channel（特征维度）的数值幅度，比其他 channel 大几十到上百倍**，而且这些"捣乱"的 channel 在不同输入下**位置是固定的**。

```
某层激活各 channel 的幅度：

channel:   0    1    2   ...  1337  ...  4095
幅度:     0.3  0.5  0.4  ...  ██ 87 ...  0.4
                              ↑ 系统性离群 channel
```

权重就规矩得多，分布接近高斯，没有这种系统性的巨大离群。

### 7.2 后果

- **权重量化到 INT4 都能接受**（有算法保护的前提下）
- **激活量化到 INT8 就很吃力**——per-tensor 直接崩，必须 per-token 或者做特殊处理

### 7.3 三种应对思路

| 思路 | 代表 | 做法 |
|---|---|---|
| 换更抗离群的数据类型 | **FP8** | 浮点有指数位，动态范围天然宽（见 2.3） |
| 把离群的难度转移走 | **SmoothQuant** | 数学等价变换，把激活的难度挪一部分给权重 |
| 保护重要的权重通道 | **AWQ** | 用激活幅度找出"重要"权重通道，量化前先放大保护 |

---

## 八、主流算法详解

### 8.1 RTN（Round-To-Nearest）——最朴素的基线

就是第三节那套公式，直接算 scale、直接 round，**不做任何补偿**。

- 优点：几秒钟跑完，不需要任何数据
- 缺点：INT8 勉强能用，INT4 通常掉点明显
- 用途：当基线、快速验证流程通不通

### 8.2 GPTQ——"让后面的权重替前面的擦屁股"

**核心直觉**：矩阵乘是 `Y = XW`。量化 `W` 会让 `Y` 产生误差。但 `W` 有很多列，**我可以在量化第 1 列之后，微调还没量化的第 2、3、4...列，让它们的取值把第 1 列造成的输出误差"补"回来。**

```
逐列处理：
  量化第 j 列  →  算出它造成的输出误差  →  把误差按最优方式摊派给第 j+1...n 列
                                            （用 Hessian 逆矩阵决定摊派权重）
```

那个"最优摊派方式"，需要知道输入 `X` 的统计特性（二阶信息，即 Hessian ≈ XᵀX）——**这就是它需要校准数据的原因**。

- 精度：W4A16 下很好
- 成本：7B 模型约 10~30 分钟（要算 Hessian 和逆）
- 对校准集的敏感度：中等偏高

### 8.3 AWQ——"1% 的权重决定 99% 的效果"

**核心观察**：权重不是同等重要的。有大约 **1% 的权重通道**，对模型输出的影响远超其他。

**第二个观察**（这是 AWQ 最聪明的地方）：**怎么判断哪些权重重要？不看权重自己的大小，要看和它相乘的那个激活 channel 的大小。** 因为一个权重再大，如果总是乘以接近 0 的激活，它对输出也没影响。

**做法**——利用矩阵乘的一个等价变换：

```
Y = X · W  =  (X / s) · (s · W)
              ↑激活缩小    ↑权重放大
```

对重要通道选一个 `s > 1`，把权重放大之后再量化。权重变大了，**相对量化误差就变小了**（因为误差上限是 scale/2，是绝对值，权重越大相对误差越小）。而激活那边除以 `s`，数学上完全等价，输出不变。

- 精度：与 GPTQ 相当，某些任务更好
- 成本：比 GPTQ 快很多（只要统计激活幅度，不用算 Hessian）
- 对校准集的敏感度：低（只用来看激活幅度的相对大小）

> **选型建议：默认先试 AWQ**（快、稳、对校准集不挑）；**掉点超预期再试 GPTQ**。

### 8.4 SmoothQuant——为 W8A8 铺路

前面两个都是 weight-only（W4A16）。要做 W8A8 就必须解决激活量化，SmoothQuant 就是干这个的。

**核心直觉**：激活难量化（有离群 channel），权重好量化（分布平坦）。那就**把难度从激活转移给权重**，让两边都"中等难"。

用的还是那个等价变换，但方向相反：

```
Y = (X / s) · (s · W)
     ↑激活除以s，离群被压平    ↑权重乘以s，变难一点但还扛得住

s_j = max|X_j|^α / max|W_j|^(1-α)      # α 通常取 0.5，控制转移多少
```

`α=0.5` 表示难度对半分。这样激活和权重都能用 INT8 量化了。

### 8.5 FP8——最省事的方案（如果硬件支持）

不做任何算法补偿，纯靠 FP8 这个数据类型本身抗离群（2.3 节）。

典型配置 **FP8_DYNAMIC**：
- 权重：per-channel 静态量化（离线算好）
- 激活：per-token 动态量化（运行时现算）
- **完全不需要校准数据**

- 精度：通常近乎无损（掉点 < 1%）
- 成本：几分钟跑完
- 限制：**必须 Ada / Hopper 及以上硬件**

> **如果你有 H100 / L40S，FP8 是性价比最高的方案，没有之一。**

### 8.6 一张对比表

| 算法 | 类型 | 要校准吗 | 量化耗时(7B) | 精度 | 硬件门槛 |
|---|---|---|---|---|---|
| RTN | W8/W4 | 否 | 秒级 | INT4 差 | 无 |
| **AWQ** | W4A16 | 是（轻） | 分钟级 | 好 | Turing+ |
| **GPTQ** | W4A16 | 是 | 10~30min | 好 | Volta+ |
| SmoothQuant | W8A8 INT8 | 是 | 分钟级 | 中上 | Turing+ |
| **FP8 Dynamic** | W8A8 FP8 | **否** | 分钟级 | **极好** | **Ada/Hopper+** |
| NVFP4 | W4A4 | 是 | 分钟级 | 好 | Blackwell |

---

## 九、算法 / 格式 / Kernel：三件不同的事

初学者极易混淆，用一张图理清：

```
  ①算法                ②格式                    ③Kernel
（怎么算量化参数）    （怎么存到磁盘）          （运行时怎么算）

  AWQ                  compressed-tensors        Marlin    (W4A16, Ampere+)
  GPTQ        ───▶     AWQ / GPTQ 格式    ───▶   Machete   (W4A16, Hopper)
  SmoothQuant          GGUF                      CUTLASS   (W8A8)
  FP8 dynamic          bitsandbytes
```

### 关于格式

- **compressed-tensors**——vLLM 官方主推的**统一格式**，llm-compressor 的产物。一种格式覆盖 W4A16 / W8A8 / FP8。**新做的量化优先选它。**
- **AWQ / GPTQ 格式**——历史悠久，HF 上现成模型最多，vLLM 完整支持。
- **GGUF**——llama.cpp 生态的格式，给本地/CPU 跑的。vLLM 支持但性能一般，**生产别用**。
- **bitsandbytes**——运行时量化，加载时现场压缩。方便但慢，**只适合快速试**。

### 关于 Kernel

**Marlin** 是 W4A16 的高性能 kernel 实现。关键点：

> vLLM 会在硬件支持时**自动把 AWQ / GPTQ 的权重重排成 Marlin 布局**，
> 启动日志里会显示 `awq_marlin` 或 `gptq_marlin`。
> **看到 marlin 就对了**——它比原生 AWQ/GPTQ kernel 快不少，你不需要手动指定。

---

## 十、硬件支持矩阵

**这张表直接决定你能选什么，先查表再选方案。**

| 方法 | Volta<br>V100 | Turing<br>**T4** | Ampere<br>A10/A100 | Ada<br>L40S | Hopper<br>H100 |
|---|:---:|:---:|:---:|:---:|:---:|
| AWQ (W4A16) | ✗ | ✓ | ✓ | ✓ | ✓ |
| GPTQ (W4A16) | ✓ | ✓ | ✓ | ✓ | ✓ |
| Marlin kernel | ✗ | ✓ | ✓ | ✓ | ✓ |
| INT8 W8A8 | ✗ | ✓ | ✓ | ✓ | ✓ |
| **FP8 W8A8** | ✗ | **✗** | **✗** | ✓ | ✓ |
| bitsandbytes | ✓ | ✓ | ✓ | ✓ | ✓ |
| GGUF | ✓ | ✓ | ✓ | ✓ | ✓ |

架构对应：Volta=SM70，Turing=SM75，Ampere=SM80/86，Ada=SM89，Hopper=SM90。

> ### ⚠️ 对你最重要的一条
> **你线上主力是 T4，FP8 这条路在 T4 和 A100 上都走不通**（没有 FP8 Tensor Core）。
> T4 上的正确选择是 **AWQ / GPTQ 的 W4A16 + Marlin kernel**；
> 想要 W8A8 就走 **INT8 SmoothQuant**。

---

## 十一、在 vLLM 上实操

### 路线 A：直接用现成的量化模型（最省事，先从这里开始）

HF 上大量模型有量化版本，命名通常带后缀：`-AWQ`、`-GPTQ`、`-FP8`、`-W4A16`。

```bash
vllm serve Qwen/Qwen2.5-7B-Instruct-AWQ \
  --max-model-len 4096 \
  --gpu-memory-utilization 0.9
```

> ### ⭐ 不需要加 `--quantization`
> vLLM 会读模型目录下 `config.json` 里的 `quantization_config` 字段**自动识别**格式，
> 并自动选择最优 kernel。手动指定反而可能禁用 Marlin，性能变差。
>
> 只有两种情况需要手动写：checkpoint 里没有量化配置（少见），
> 或者你想强制降级 kernel 做 A/B 对比。

**启动后必看的日志**：

```
INFO ... Using quantization config: awq_marlin       ← 确认识别到了，且用上了 marlin
INFO ... # GPU blocks: 12043, # CPU blocks: 2048     ← 和 FP16 版本对比，block 数应显著变多
INFO ... Maximum concurrency for 4096 tokens: 47.0x  ← 并发能力
```

**第一件事就是和 FP16 版本对比 GPU blocks 数**——如果没明显变多，说明量化没生效或者显存被别的东西吃了。

### 路线 B：用自己的业务数据做量化

工具链统一用 **llm-compressor**（vLLM 官方主推，产物是 compressed-tensors 格式）：

```bash
pip install llmcompressor
```

#### B-1：W4A16（T4 / A10 / A100 上的主力方案）

```python
from datasets import Dataset
from transformers import AutoModelForCausalLM, AutoTokenizer
from llmcompressor import oneshot
from llmcompressor.modifiers.quantization import GPTQModifier

MODEL_ID = "Qwen/Qwen2.5-7B-Instruct"
SAVE_DIR = "Qwen2.5-7B-Instruct-W4A16-G128"

NUM_CALIBRATION_SAMPLES = 512      # 256~1024 都合理，512 是常用值
MAX_SEQUENCE_LENGTH = 2048         # 按你业务的真实长度配

model = AutoModelForCausalLM.from_pretrained(MODEL_ID, torch_dtype="auto")
tokenizer = AutoTokenizer.from_pretrained(MODEL_ID)

# ⭐⭐ 最关键的一步：校准集用线上真实请求，不要用 ultrachat / wikitext
#     理由见下面「为什么校准集这么重要」
raw = [{"messages": [{"role": "user", "content": q}]} for q in your_online_queries[:512]]
dataset = Dataset.from_list(raw)

def preprocess(ex):
    # 必须用和线上完全一致的 chat template，否则分布对不上
    return {"text": tokenizer.apply_chat_template(ex["messages"], tokenize=False)}

def tokenize(ex):
    return tokenizer(ex["text"], padding=False,
                     max_length=MAX_SEQUENCE_LENGTH,
                     truncation=True, add_special_tokens=False)

dataset = dataset.map(preprocess).map(tokenize, remove_columns=["messages", "text"])

recipe = GPTQModifier(
    targets="Linear",        # 只量化线性层（注意力和 MLP 的权重矩阵）
    scheme="W4A16",          # group_size 默认 128
    ignore=["lm_head"],      # ⚠️ 输出层必须排除，量化它掉点很明显
)

oneshot(
    model=model,
    dataset=dataset,
    recipe=recipe,
    max_seq_length=MAX_SEQUENCE_LENGTH,
    num_calibration_samples=NUM_CALIBRATION_SAMPLES,
)

model.save_pretrained(SAVE_DIR, save_compressed=True)
tokenizer.save_pretrained(SAVE_DIR)
```

直接起服务，格式会被自动识别：

```bash
vllm serve ./Qwen2.5-7B-Instruct-W4A16-G128
```

> **想用 AWQ 而不是 GPTQ**：llm-compressor 也提供 `AWQModifier`，
> 用法类似但更快。不确定的话先用上面的 GPTQ 跑通流程，再换 AWQ 对比掉点。

#### B-2：FP8（有 L40S / H100 时用这个）

**不需要校准数据，几分钟跑完**：

```python
from transformers import AutoModelForCausalLM, AutoTokenizer
from llmcompressor import oneshot
from llmcompressor.modifiers.quantization import QuantizationModifier

MODEL_ID = "Qwen/Qwen2.5-7B-Instruct"
OUTPUT_DIR = "Qwen2.5-7B-Instruct-FP8-Dynamic"

model = AutoModelForCausalLM.from_pretrained(MODEL_ID, torch_dtype="auto")
tokenizer = AutoTokenizer.from_pretrained(MODEL_ID)

recipe = QuantizationModifier(
    targets="Linear",
    scheme="FP8_DYNAMIC",    # 权重 per-channel 静态 + 激活 per-token 动态
    ignore=["lm_head"],
)

oneshot(model=model, recipe=recipe)   # 注意：没有 dataset 参数

model.save_pretrained(OUTPUT_DIR)
tokenizer.save_pretrained(OUTPUT_DIR)
```

#### 为什么校准集这么重要

校准数据的作用是**让算法看到"真实的激活分布长什么样"**：

- GPTQ 用它算 Hessian，决定误差怎么摊派
- AWQ 用它找哪些通道重要
- SmoothQuant 用它算难度转移系数 `s`

如果你用通用语料（wikitext、ultrachat）校准，但线上跑的是**搜索 Query 改写**这种短文本、领域词多的输入，**两者的激活分布对不上**，算出来的 scale 和保护策略就是针对错误分布优化的，掉点会明显高于用业务数据。

**实践要点**：
- 从**线上日志**采样，覆盖真实的长度分布和内容分布
- 数量 256~1024 条，再多收益递减
- **必须套上线上真实的 prompt 模板 / chat template**
- 如果有多个业务共用一个模型，按流量比例混合采样

### 路线 C：KV Cache 量化（独立开关，可与 A/B 叠加）

```bash
vllm serve ./your-model --kv-cache-dtype fp8_e4m3
```

| 取值 | 说明 |
|---|---|
| `auto` | 默认，不量化 |
| `fp8` | FP8 KV Cache，**不校准时所有 scale 默认为 1.0** |
| `fp8_e4m3` | 精度优先，CUDA 11.8+ / ROCm |
| `fp8_e5m2` | 范围优先，CUDA 11.8+ |

- **不需要校准**就能用，一般够用；要极致精度可用 llm-compressor 生成校准过的 scale，还能开 per-attention-head 量化（需要 Flash Attention 后端）
- 收益：**并发数近似翻倍**，长上下文场景尤其明显
- ⚠️ 滑动窗口注意力对 KV 量化更敏感，可用 `--kv-cache-dtype-skip-layers` 指定层名或索引保留不量化

---

## 十二、怎么验证量化做对了

**量化最危险的地方是"看起来能跑"**——模型不报错、输出也通顺，但业务指标悄悄掉了 3 个点。必须做两件事。

### 12.1 精度验证：看业务指标，不看 PPL

> ### ⚠️ 不要用 PPL（困惑度）当验收标准
> PPL 掉 0.1 看起来微不足道，业务指标可能掉 3 个点。PPL 是语言建模的平均指标，
> **对你关心的那类输入不敏感**。

正确做法：

1. 准备一个**业务评测集**（和你平时评估模型用的同一个）
2. FP16 原模型跑一遍，存为基线
3. 量化模型跑一遍，逐条对比
4. 看**业务指标**（准确率 / 召回率 / BLEU / 人工评分），不看 PPL
5. **抽样人工看长尾**——量化的典型故障模式是"平均分没变，但少数输出开始胡说"

对生成任务，还可以做**逐 token 对拍**：同样的输入、`temperature=0`，看两个模型的输出在第几个 token 开始分叉。分叉得越晚越好。

### 12.2 性能验证：确认真的变快了

```bash
vllm bench serve \
  --model ./your-model \
  --dataset-name random \
  --random-input-len 512 \
  --random-output-len 128 \
  --request-rate 10 \
  --num-prompts 500
```

> ### ⭐ 输入输出长度必须按线上真实分布配
> 这是最容易被忽略的一点。用 `input=128, output=512`（decode 重）测出来 W4A16 提升 3 倍，
> 换成 `input=1024, output=32`（prefill 重）可能提升是 0 甚至负的。
> **口径不对，结论就是错的。**

要看的指标：

| 指标 | 含义 | 量化后的预期 |
|---|---|---|
| **TTFT** | 首 token 延迟 | W4A16 基本不变或略差；W8A8 会改善 |
| **TPOT / ITL** | 每输出 token 耗时 | W4A16 明显改善 |
| **吞吐 (tok/s)** | 整体 | 看 workload |
| **GPU blocks** | 启动日志里的 KV Cache 容量 | 应显著变多 |
| **最大并发** | | 应显著变高 |

---

## 十三、高频坑

1. **量化了反而变慢**
   → workload 是 prefill 重 / batch 大，weight-only 的反量化开销盖过带宽收益。**先判断 prefill/decode 谁重，再决定量化方案。**

2. **手写 `--quantization` 导致性能腰斩**
   → 绝大多数情况不用写，让 vLLM 自动检测。写错会禁用 Marlin。**看启动日志确认是不是 `xxx_marlin`。**

3. **校准集用通用语料**
   → 激活分布对不上，scale 估偏。**这是掉点的头号原因。**

4. **`lm_head` 忘了 ignore**
   → 输出层直接决定 token 概率分布，量化它对生成质量的影响不成比例地大。

5. **在 T4 / A100 上试 FP8**
   → 硬件不支持，报错或静默回退到未量化。查第十节的表。

6. **用 PPL 验收**
   → 见 12.1。

7. **量化后忘了重新调 `gpu_memory_utilization` 和并发参数**
   → 量化省出来的显存如果不调 `max_num_seqs` / `max_model_len`，**收益等于白拿**。量化之后一定要重新做一轮参数调优。

8. **MoE 模型直接套用稠密模型的配方**
   → MoE 的 expert 权重量化策略不同（有些 expert 激活频率极低），且路由层通常要保留高精度。

---

## 十四、决策表与术语表

### 14.1 我该选哪个方案

| 你的情况 | 选什么 |
|---|---|
| **T4 / A10 / A100**，decode 重、batch 小 | **AWQ 或 GPTQ W4A16**（自动走 Marlin） |
| **T4 / A10 / A100**，大 batch 高吞吐 | **INT8 W8A8**（SmoothQuant） |
| **L40S / H100** | **FP8 W8A8 dynamic**——无需校准、近乎无损，首选 |
| 显存不够、并发上不去 | 叠加 `--kv-cache-dtype fp8_e4m3` |
| 只想快速看看效果 | HF 上找现成的 `-AWQ` / `-FP8` checkpoint（路线 A） |
| 精度要求极高、延迟不敏感 | 不量化，或只量化 KV Cache |
| 本地笔记本跑着玩 | GGUF + llama.cpp（不是 vLLM 的场景） |

### 14.2 术语表

| 术语 | 含义 |
|---|---|
| **PTQ** | Post-Training Quantization，训练后量化。LLM 场景 99% 用这个 |
| **QAT** | Quantization-Aware Training，量化感知训练。要重训，LLM 上成本不可接受 |
| **scale** | 缩放因子，量化的核心参数 |
| **zero_point** | 零点偏移，非对称量化才有 |
| **outlier** | 离群值，量化的头号敌人 |
| **calibration** | 校准，用少量真实数据统计激活分布来确定量化参数 |
| **W4A16** | 权重 4bit、激活 16bit |
| **per-group** | 每 N 个元素（通常 128）共享一个 scale |
| **Marlin** | W4A16 的高性能 CUDA kernel |
| **compressed-tensors** | vLLM 主推的统一量化格式 |
| **llm-compressor** | 生成 compressed-tensors 的官方工具链 |
| **memory-bound** | 访存受限，瓶颈在搬数据。decode 阶段的状态 |
| **compute-bound** | 计算受限，瓶颈在算力。大 batch prefill 的状态 |

### 14.3 建议的学习路径

1. **跑通路线 A**——下载一个现成的 `-AWQ` 模型，起 vLLM，看启动日志里的 GPU blocks 和 FP16 版本差多少。**先建立感性认识。**
2. **跑通路线 B**——拿一个小模型（1.5B）+ 你的业务数据做一次 W4A16，走完"量化 → 起服务 → 精度对拍 → 压测"的完整闭环。
3. **做一次对比实验**——同一个模型的 FP16 / W4A16 / W8A8 三个版本，在**两种不同的输入输出长度配置**下分别压测。你会亲眼看到"量化不一定变快"。
4. **读论文**（按推荐顺序）：SmoothQuant → AWQ → GPTQ。AWQ 那篇最好读，思想也最漂亮。
5. **回到 [01-大模型推理.md](01-大模型推理.md) 的 Q0 和 Q10**，把这些理解压缩成面试能讲的 60 秒版本。

---

## 附：和其它文档的关系

| 想干什么 | 看哪 |
|---|---|
| 系统学量化 | **本文档** |
| 面试被问"你怎么调优" | [01-Q0](01-大模型推理.md) 三层方法论 |
| 面试被问"量化精度损失从哪来" | [01-Q10](01-大模型推理.md) |
| 确认简历上量化那句敢不敢写 | [05-自查清单.md](05-自查清单.md) 技能栏自查 |
