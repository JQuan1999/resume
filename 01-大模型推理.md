# 01 · 大模型推理（必背）

---

## 〇、调优方法论（总纲）⭐ 简历写了"三大框架的部署与调优"，这题必被问

### Q0. 你说熟悉 vLLM / SGLang / TensorRT-LLM 的调优，具体怎么调的？

⚠️ **这是转岗者最容易翻车的一题。** 真实情况是：你的调优很大一部分靠实验——
换框架对比、量化后看业务收益、参数试几组挑最好的。这本身没问题，
**问题在于把它说成"试"。** 面试官问"怎么调优"，真正想听的是「**你怎么定位瓶颈**」，
实验只是验证手段，不是方法本身。

**答法：拆成三层，越往下越硬。** 诚实承认第一层靠实验，用第二三层证明不止有实验。

#### 口播稿（60~90 秒）

> 我的调优分三层，越往上越依赖实验，越往下越依赖分析。
>
> **第一层是后端选型。** 这层确实主要靠实测对比，但对比有固定口径：同一个模型、
> 同一份从线上采样的真实输入输出长度分布、固定并发梯度、固定显存预算，
> 三个框架**各自调到自己的最优点再比**，指标分开看 TTFT、TPOT、吞吐三条曲线，
> 而不是只看一个平均延迟。
>
> 我们内部能这么比，是因为我把三个后端封成了统一镜像和统一参数面——
> TP、max_batch_tokens、max_num_seqs、gpu_memory_utilization、max_model_len
> 全走同一套环境变量，业务方换后端零改造。所以这个对比是 apples-to-apples 的，
> 不是各调各的然后比一个数。
>
> **第二层是参数调优。** 这层我不盲扫，先判断 workload 卡在哪：
> 先算 KV Cache 的显存账，看 `gpu_memory_utilization` 留出来的空间能放多少条并发；
> 再看输入输出比例——输入长输出短偏 prefill-bound，就往 chunked prefill 和
> `max_batch_tokens` 调；输出长偏 decode-bound，就往 `max_num_seqs`、CUDA Graph、
> 前缀缓存调。**判断完再扫参数，扫描是为了验证判断**，一般两三组就收敛。
>
> **第三层是代码级优化，完全靠 profiling。** 比如 GR 生成式检索那个场景，
> 我 profiling 发现解码热路径上每个 step 都把 15 万维全词表 logits 拷回 CPU 做
> log_softmax，改成全程 GPU 计算、显存驻留、末尾一次 gather 取回标量，
> 每请求的设备同步次数从 O(生成长度) 降到 1 次。
>
> 所以：**选型靠实验、参数靠显存账加瓶颈判断、代码靠 profiling。**
> 我也直说，CUDA kernel 层面的优化我目前经验还不多，这是我在补的方向。

#### 必须紧接着补的一刀：用机制而非压测数字解释选型

单说"哪个快用哪个"会被判定为没理解机制。选型理由见 **Q7**，然后立刻接上这段——
**它是你唯一一个"基于框架内部机制做选型"的真实案例，价值远高于任何压测数字**：

> 我们 GR 场景最后选 TensorRT-LLM 的 C++ backend，不是因为压测它快，
> 是因为 PyTorch backend 的 logits processor 只接收 beam0，没法按 beam 施加独立 mask，
> 而我们要做 Trie 约束的 beam search，只有 C++ backend 能做到。
> 顺带踩的坑是 beam width 要烘焙进 engine（max_beam_width），编译期就得定。

#### 量化部分的标准答法（坦白只用开源工具，但把 trade-off 讲透）

> 量化我用的是开源工具链，没有自己实现量化算法。流程是：**校准集用业务真实 query
> 抽样几百条，不用 wikitext 这类通用语料**——校准集分布和线上分布不一致，
> 激活的 scale 就估偏了。跑完之后两件事必做：一是在业务评测集上做精度对拍，
> 看业务指标掉多少，**不看 PPL**，PPL 掉一点点业务指标可能掉很多；
> 二是重新压测，确认吞吐真的涨了。
>
> 踩过的坑是：**量化不等于一定变快。** W4A16 这类 weight-only 量化，
> 收益在 decode 阶段——decode 是 memory-bound，瓶颈在权重读取，压缩权重直接受益；
> 但大 batch 的 prefill 阶段是 compute-bound，反量化还有额外开销，可能反而更慢。
> 所以得先看 workload 的输入输出比例，再决定值不值得量化。

**这段是整个回答里性价比最高的**——它把"我只会用工具"变成了
"我用工具，但知道它什么时候不管用"。用工具没人扣分，不理解 trade-off 才扣分。
原理层追问见 **Q10**。

#### 必被追问（每个 30 秒内答完）

| 追问 | 要点 | 详见 |
|---|---|---|
| 你怎么压的？ | 并发梯度、输入输出长度分布来源（线上采样 or 定长）、跑多久、预热几轮、TTFT 还是端到端、P99 还是均值。**口径答不上来前面全白说** | Q6 追问 |
| `gpu_memory_utilization` 调高有什么风险？ | 挤掉 activation 和 CUDA Graph 的显存，长请求 OOM 或频繁抢占重算 | Q4 |
| `max_num_seqs` 调大一定吞吐涨吗？ | 不一定。KV Cache 放不下会触发 preemption，重算开销吃掉收益；且 batch 变大 TPOT 升，延迟敏感场景更差 | Q4 / Q5 |
| per-tensor vs per-channel？ | per-tensor 一个离群值毁整张量；per-channel/per-group 精度好、开销略大。激活量化难点就是 outlier | Q10 |
| chunked prefill 解决什么？ | 牺牲一点 TTFT 换 TPOT 平滑 | Q6 |

#### 三条禁忌

1. ❌ **别说"哪个好用用哪个"**——这句话本身就是"没有方法论"的自白。
   换成"对比口径是 X，结论是 Y，因为 Z"。
2. ❌ **别编数字**。压测口径一追就穿。答"这个数我记不准了，但结论是 X 比 Y
   在该场景好一个量级 / 好百分之几十"完全可以接受。
3. ❌ **别把没做过的说成做过**。"CUDA kernel 我经验不多，在补"说出来不扣分，
   装懂被戳穿才扣分。你的准确边界是：**写过 Triton 的 matmul，理解 tile 划分，
   但没在生产里做过 kernel 级优化**。

#### 顺带：简历这句话可以加固

原句（`resume_v2.md` 技能栏）：

> 熟悉 vLLM / SGLang / TensorRT-LLM 三大框架的部署与调优

可改成：

> 熟悉 vLLM / SGLang / TensorRT-LLM 的部署、参数调优与**基于 workload 特征的后端选型**，
> 具备基于 profiling 的定制化解码开发经验

改动很小，但把"调优"从一个空词变成了有指向的能力，也和上面的三层答法对上了。

---

## 一、KV Cache 与显存管理

### Q1. PagedAttention 解决什么问题？

**短答**：解决 KV Cache 的显存碎片和过度预留。

**展开**：
传统做法给每个序列预留一块连续显存，按 `max_seq_len` 申请。问题有三个：
一是内部碎片——实际生成 100 token 却按 2048 预留；
二是外部碎片——序列长短不一，释放后剩下的空洞用不上；
三是无法共享——多个请求的相同前缀各存一份。

PagedAttention 借鉴操作系统虚拟内存分页：把 KV Cache 切成固定大小的 block（比如 16 个 token
一块），序列维护一张 block_table 做逻辑块到物理块的映射，物理块可以不连续。
于是预留粒度从"整个序列"降到"一个 block"，浪费上限就是最后一块的半块；
相同前缀的 block 还能通过引用计数被多个序列共享。

**追问：block_size 怎么选？**
> 小了 block_table 变长、寻址开销和 kernel 里的间接访问变多；
> 大了内部碎片回升、前缀缓存的命中粒度变粗（必须凑满整块才算命中）。
> 一般 16 或 32。vLLM 默认 16。

---

### Q2. 前缀缓存（Prefix Caching）是怎么命中的？⭐ 你的送分题

**短答**：对每个完整 block 算一个**链式哈希**，用 hash 做全局索引表查已有物理块。

**展开（照着你读过的 nano-vLLM 说）**：
```python
h = compute_hash(token_ids_of_block_i, prefix=h_of_block_{i-1})
```
关键是 `prefix` 参数——每块的哈希把**前一块的哈希**也喂进去。这样哈希值编码的不是
"这一块的内容"，而是"从序列开头到这一块为止的整个前缀"。两个请求只有在前缀完全相同时
哈希才会一路相同，就不会出现"中间某块内容碰巧一样就误判命中"的问题。

命中流程：
1. `can_allocate` 遍历序列的前 N-1 个**完整**块（最后一块正在填充，不参与缓存），
   逐块算链式哈希，在 `hash_to_block_id` 里查；
2. 查到了还要再比一次 `block.token_ids != token_ids` ——**防哈希碰撞的兜底校验**；
3. 命中的块数记为 `num_cached_blocks`，同时统计真正需要新分配的块数，
   不够就返回 -1 让调度器排队；
4. `allocate` 时命中块 `ref_count += 1` 直接复用，未命中部分才从空闲队列取；
5. 最后设置 `seq.num_cached_tokens = num_cached_blocks * block_size`，
   **prefill 阶段直接跳过这部分 token 的计算**——省的是算力，不只是显存。

**追问：为什么最后一块不参与缓存？**
> 因为它还没填满，内容还会变。只有写满且内容固定的块，哈希才是稳定的、可被复用的。

**追问：块被复用了，原来那个序列改写它怎么办？**
> 不会改写。命中的块都是已经写满的历史块，KV 值不再变化；新生成的 token 只会写入
> 序列自己新分配的块。共享的是只读的历史前缀。

**追问：缓存块什么时候被淘汰？**
> `ref_count` 归零后进空闲队列，但 hash 还留着，块仍可被命中复用（"软"缓存）；
> 真正被再次分配出去时，才把旧 hash 从索引表里删掉。
> 空闲队列是 FIFO，近似 LRU 的效果。

**这题一定要主动带出你写过的验证脚本**：
> 我为了确认这套逻辑，给 BlockManager 的 can_allocate / allocate / hash_blocks
> 打了猴子补丁做插桩，跑了冷启动、完全命中、部分命中三种场景，
> 把每次调用的命中块数和 block_table 打出来对照，还测了 block 耗尽时的行为。

---

### Q3. `can_append` 为什么是 `len(seq) % block_size == 1`？

**短答**：判断这次 decode 是不是刚好要开新块。

**展开**：
decode 每步加一个 token。当序列长度对 block_size 取模等于 1 时，说明这个新 token 是
某个块的第一个 token，必须新分配一块；其余情况都是往当前块里填，不需要新块。
Python 里 `True == 1`，所以 `len(free) >= (条件)` 就统一表达了"需要新块时至少要有 1 个空闲块，
不需要时恒成立"。

**追问：抢不到块会怎样？**
> 触发抢占。vLLM 的做法是把某个序列的块全部释放（recompute 模式，后面重新 prefill，
> 有前缀缓存的话代价没那么大）或者 swap 到 CPU 内存。
> 抢谁一般按先来后到的反序，保证老请求先完成。

---

### Q4. 显存怎么算？`gpu_memory_utilization` 是什么？

> 一张卡的显存大致分四块：模型权重、KV Cache、激活值 / 临时 buffer、框架和 CUDA context 开销。
> `gpu_memory_utilization`（vLLM 默认 0.9，我们线上配 0.92）是框架允许占用的显存总比例，
> 框架先加载权重、跑一次 profile 拿到激活峰值，**剩下的全部划给 KV Cache**，
> 除以每 token 的 KV 大小就是能缓存的 token 总数。
>
> 每 token 的 KV 字节数 = `2（K和V） × 层数 × KV头数 × head_dim × 数据字节数`。
> GQA 就是把 KV 头数压下来，这也是为什么 GQA 对推理显存收益巨大。

**追问：`max_model_len` 调大会怎样？**
> 不直接吃显存，但它决定单请求最坏情况能占多少 KV block，
> 会压低框架敢同时调度的并发数，表现为吞吐下降。
> 调大之前要确认业务真实的输入输出长度分布，别为了极少数长请求牺牲整体吞吐。

⚠️ **这题一定要能算**。面试官很爱问"7B 模型 A10 上能跑多长上下文"，
现场算不出来会很尴尬。提前拿你线上跑的模型算一遍，记住数量级。

---

## 二、调度与批处理

### Q5. Continuous Batching 和你在 inferserving 里做的动态 batch 有什么区别？⭐ 你的送分题

**这题是你的优势题，因为你两边都做过。**

> 本质区别是**批的生命周期**。
>
> 我在 inferserving 里做的是**请求级**的静态凑批：从无锁队列批量出队，凑够
> `max_batch_size` 就走，不够就按 `qtimeout` 分段续取，凑齐一批一起进 TensorRT，
> 这一批同进同出。这对小模型（bert 排序、embedding）是合适的——
> 单次推理是毫秒级、输出定长，批内不存在"有的早结束有的还没完"。
>
> LLM 不一样：每个请求要 decode 几十上百步，长度还不一样。如果也同进同出，
> 短请求得等最长的那个，GPU 大量时间在算 padding。Continuous Batching 是**迭代级**调度：
> 每个 decode step 结束后重新组批，完成的请求立刻出队、等待的请求立刻插进来，
> batch 的成员每一步都在变。
>
> 所以两者的调度单位不同：我那套是"一个请求进一次批"，LLM 是"一个 token step 组一次批"。

**追问：那你的凑批策略，延迟和吞吐怎么权衡的？**
> `qtimeout` 就是这个旋钮。等得越久批越大、吞吐越高，但队头请求的延迟被拉长。
> 我的实现是先用 `wait_dequeue_bulk` 非阻塞地尽量取满，不足 `min_batch_size` 时
> 再用 `wait_dequeue_bulk_timed` 按 `qtimeout/3` 分三次续取——
> 分段而不是一次等满，是为了让批在中途凑齐就能提前走，不白等。

---

### Q6. Chunked Prefill 解决什么问题？

> 解决 prefill 和 decode 抢 GPU 的问题。
>
> prefill 是计算密集的（一次算几百上千 token，算力打满），decode 是访存密集的
> （一次算一个 token，算力闲着、带宽打满）。如果一个长 prompt 的 prefill 独占一轮，
> 所有正在 decode 的请求这一轮都被卡住，表现为 **TPOT 尖刺**。
>
> Chunked Prefill 把长 prompt 切成若干 chunk，每一轮只算一个 chunk，
> 剩下的预算拿去和 decode 请求混批。好处是 decode 的 ITL 平稳了，
> 而且 prefill 的算力和 decode 的带宽正好互补，整体 GPU 利用率更高。
> 代价是长 prompt 的 TTFT 会变长。

**追问：TTFT / TPOT / ITL 分别是什么？**
> TTFT 首 token 延迟，主要由 prefill 和排队决定，面向"用户多久看到第一个字"；
> TPOT 每输出 token 的平均耗时；ITL 是相邻 token 的间隔。
> 流式场景真正影响体感的是 TTFT + ITL 的稳定性，端到端延迟反而次要。
>
> ⚠️ 压测报数据时一定要说清楚是哪个指标、输入输出长度多少、并发多少——
> 不说清楚口径的延迟数字没有意义。

---

### Q7. vLLM / SGLang / TensorRT-LLM 怎么选？

**照着你的真实工作答**：

> 我们的做法是三个都封进统一镜像，用环境变量统一调参面，让业务按场景选。
>
> - **vLLM**：生态最全、模型支持最快、社区最活跃。新模型上线、快速验证首选，
>   我们大部分场景默认走它。
> - **SGLang**：RadixAttention 的前缀缓存做得更激进，对**多轮对话、共享长 system prompt、
>   结构化输出**这类前缀重复率高的场景收益明显。
> - **TensorRT-LLM**：需要离线编译 engine，灵活性最差、迭代成本最高，
>   但在固定模型 + 固定 shape + 追求极致延迟的场景下性能最好，
>   而且它的 C++ runtime 能力更完整——比如我做约束解码时就发现，
>   按 beam 施加不同 logits 掩码只有走它的 C++ backend 才做得到。
>
> 选型的实际标准：模型迭代快就 vLLM，前缀复用高就 SGLang，
> 模型定死、延迟卡死、愿意付编译成本就 TRT-LLM。

⚠️ **别说"TRT-LLM 最快所以最好"**——面试官会追问"那你们为什么大部分用 vLLM"。
工程选型的答案永远是 trade-off，不是排名。

---

### Q8. 张量并行（TP）是怎么切的？通信量多大？

> 按权重矩阵切。Transformer 里主要是两处：
> - **MLP**：第一个线性层按列切（ColumnParallel，每卡算一部分输出维度），
>   第二个按行切（RowParallel），中间不需要通信，第二个算完做一次 **AllReduce**。
> - **Attention**：按 head 切，每卡负责若干个 head，输出投影按行切，再一次 AllReduce。
>
> 所以每个 Transformer layer 有 2 次 AllReduce，通信量是 `batch × seq × hidden × 2字节`。
> decode 阶段 seq=1，通信量小但**次数多、延迟敏感**，所以 TP 强依赖卡间带宽——
> NVLink 上 TP=8 还划算，走 PCIe 的话 TP 超过 2~4 收益就被通信吃掉了。

**追问：TP 和 PP 的区别？**
> TP 切单层内部，每层都要通信，延迟低但带宽要求高，适合单机多卡；
> PP 按层切到不同卡，通信量小（只传层间激活）但有流水线气泡，适合跨机。
> 单机优先 TP。

---

### Q9. CUDA Graph 为什么能加速？什么时候失效？

> decode 阶段每步只算一个 token，kernel 本身很小，但一层就有十几个 kernel launch，
> 几十层下来每步几百次 launch，**CPU 端的 launch 开销甚至超过 GPU 计算时间**，
> 表现为 GPU 利用率低但延迟下不来。
>
> CUDA Graph 把一整轮的 kernel 序列录制成一张图，之后一次提交整张图，
> 把几百次 launch 压成一次，CPU 开销几乎归零。
>
> 失效场景：图要求**shape 和执行路径固定**。所以只能对若干个固定的 batch size
> 预先捕获多张图（比如 1/2/4/8/16），实际 batch 向上取整到最近的一张；
> prefill 阶段 shape 千变万化，一般不用图。
> 调试时 `enforce_eager=True` 关掉它，因为图捕获会让报错栈完全失真。

---

## 三、量化与加速

### Q10. INT8 / FP8 量化的精度损失从哪来？per-tensor 和 per-channel 的区别？

⚠️ **简历技能栏写了量化，这题必须能答，答不了就把"量化"从技能栏删掉。**

> 损失来自**动态范围压缩**。把 FP16 映射到 INT8 只有 256 个格子，
> 缩放因子由这组数的最大绝对值决定。如果分布里有离群值（outlier），
> 缩放因子被它拉大，绝大多数正常值被挤进很少的几个格子里，量化误差就爆了。
> LLM 的激活值里离群值特别常见，这也是为什么**权重量化比激活量化容易得多**。
>
> - **per-tensor**：整个张量一个 scale。最省、最快，但一个离群值毁掉整个张量。
> - **per-channel**（权重按输出通道 / 激活按 token）：每个通道一个 scale，
>   离群值只影响它所在的那一个通道。精度好很多，代价是 scale 要参与 kernel 计算，
>   实现更复杂。
>
> 实践上权重普遍 per-channel，激活 per-token 动态量化。
> AWQ 的思路是识别出对输出影响大的"显著权重通道"，量化前先做等价缩放把它们保护起来；
> GPTQ 是逐层做二阶误差补偿，量化一列后把误差摊到还没量化的列上。
>
> FP8 比 INT8 好在它是浮点，有指数位，动态范围天然更宽，对离群值更鲁棒，
> 所以 FP8 常常能做到近乎无损，但需要 Hopper 及以上的硬件支持。

**追问：KV Cache 量化呢？**
> KV Cache 往往比权重还占显存。量化到 INT8/FP8 能近乎翻倍并发，
> 但 K 和 V 的敏感度不同——一般 V 更能扛，K 量化对精度影响更大，
> 所以有只量化 V 或者对 K 用更细粒度的做法。

⚠️ **如果你实际只做了"用框架的量化开关 + 调参"，就老实说**：
> 我做的是应用层：用框架提供的量化方案在我们的业务数据上做精度回归，
> 确认掉点在可接受范围后再上线，原理层面我理解到刚才说的程度，
> 但自己没实现过量化算法。

这么说完全不减分，硬编被追问"你 calibration 数据集怎么选的、掉了几个点"会当场崩。

---

### Q11. TensorRT engine 为什么必须和硬件绑定？动态 shape 怎么处理？

> 因为 TRT 在构建期做的是**面向具体硬件的自动调优**：它会针对当前 GPU 的 SM 架构、
> 显存带宽、Tensor Core 规格，把候选 kernel 实测一遍选最快的，还要做层融合、
> 精度选择和显存复用规划。这些决策全部烘焙进 engine，
> 所以换卡型、换 TRT 版本、甚至换驱动都可能不兼容，必须重新构建。
>
> 动态 shape 通过 **optimization profile** 处理：构建时给每个动态维度指定
> min / opt / max 三个值，TRT 针对 opt 那组选 kernel。所以 opt 要设成线上最典型的 shape，
> 设偏了性能会明显下降。范围跨度太大时就配多个 profile，运行时按实际 shape 选。

**结合你的真实经历补一句**：
> 我们线上是 K8s 多副本，engine 构建很慢，早期出现过多个 Pod 同时抢着 build
> 的竞争问题，后来改成支持带 shape 配置启动时直接构建 engine cache，
> 把构建从"第一次请求时"提前到"启动时"，并解决了并发 build 的竞争。

---

## 四、GR 约束解码（简历上那条 bullet）

### Q12. 展开讲讲那个 Trie 约束解码？

**结构化回答：任务 → 约束 → 框架限制 → 实现 → 瓶颈 → 结论**

> 场景是生成式检索：不生成自然语言，而是生成文档的 SID（结构化标识符）。
> 要求生成的 SID 必须是库里真实存在的，所以解码每一步都要把 logits 里
> 不合法的 token 屏蔽掉，只保留 Trie 当前节点的合法子节点。同时要用 beam search 取 top-k，
> 并且线上的打分口径要和离线 HF 评估完全一致。
>
> 第一个坑是框架选型。我先在 TRT-LLM 的 PyTorch backend 上做，
> 发现它的 logits processor **只接收 beam0 的 logits**，
> 而 beam search 里每条 beam 走到 Trie 的不同节点、合法 token 集合是不同的，
> 拿 beam0 的约束套到所有 beam 上是错的。所以必须走 **TRT / C++ backend**，
> 它的 batched logits processor 能拿到完整的 beam 维度。
> 连带的约束是 `max_beam_width` 必须在 **engine 构建期**烘焙进去，
> 所以流程被迫拆成"离线构建 engine + 在线加载服务"两段。
>
> 实现上，Trie 用平铺数组存（first_child / child_count / edge_token 几个 numpy 数组），
> 不用指针结构，避免 Python 对象开销和随机访存。
> 打分要同时算两个量：avg_lp 用 **masking 前**的 log_softmax，
> seq_score 用 **masking 后**逐步的最大 log_prob 累加——
> 这两个必须分别取，因为 masking 会把大量 token 置成 -inf，
> 算完 softmax 分布完全不同。

### Q13. 你说的那个 D2H 瓶颈，具体是怎么回事？怎么定位的？⭐ 核心亮点

> 最初的实现是每个 decode step 把整个词表的 logits 拷回 CPU 算 log_softmax。
> 词表是 151936 维，masking 前后各一次，**每步两次 D2H**。
>
> 问题不只是拷贝的字节数，更致命的是 **D2H 是一个强制同步点**：
> GPU 还在算后面的 kernel，CPU 一旦要读结果就必须等整个流排空。
> 本来 decode 是流水线执行的，这一下变成每步都强行同步一次，
> 流水线完全断掉，GPU 出现大量空泡。
>
> 定位方式是【补：你实际用的——nsys profile / torch profiler / 还是手工打时间戳分段计时】，
> 看到 decode 每步的 GPU 时间远小于墙钟时间，中间是同步等待。
>
> 改法：整个 logits processor 放进 `torch.cuda.ExternalStream(stream_ptr)` 里执行，
> log_softmax 在 GPU 上算，中间结果**留在显存**不回传，
> 只在生成结束后按最终输出的 token 做一次 gather，把两个标量取回来。
> 设备同步次数从 O(生成长度) 降到 **1 次/请求**。

**追问：那为什么最后还是没达标？剩下的瓶颈在哪？**⭐ 这题答得好，价值超过"上线了"

> 剩下三块，都还在 Python 层的解码热路径上：
> 1. **allow_mask 每步重新分配**：每个 step 都 `torch.zeros(151936, dtype=bool)` 建一个新掩码
>    张量，分配本身有开销，且没有复用。
> 2. **索引列表的 H2D**：`allow_mask[list(allowed_tokens)] = True` 用 Python list 做索引，
>    这个 list 要从 host 传到 device，又是一次隐式的 host-device 交互。
> 3. **beam=5 的放大**：每步要对 5 条 beam 各查一次 Trie 再求并集，
>    这部分集合运算在 Python 层做，GIL 内串行。
>
> 另外我为了算 avg_lp，把每步的 log_softmax 张量留在显存，
> 66 步 × 151936 × 4 字节 ≈ 40MB/请求，batch 一上去显存压力很大，
> 这是当时那版为了消除 D2H 做的空间换时间的取舍。
>
> 要继续做的话方向是：预分配一块常驻的 mask buffer 反复复用、
> 把索引改成常驻显存的 index tensor、把 Trie 查询和掩码构建下沉到 C++ 侧，
> 或者从业务侧缩短 SID 长度、降低 beam width。
> 但当时评估下来投入产出比不划算，我出了评估报告，方案没有继续推。

⚠️ **"我给出了带数据的否定结论"要说得理直气壮，不要有歉意。**
成熟团队里，能用数据否掉一个方向是正面能力。语气上别说"可惜没做成"，
要说"我把结论测清楚了，避免了后面更大的投入"。

---

### Q14. 为什么 beam width 要在编译期确定？

> 因为 TRT engine 在构建期就要把所有 tensor 的 shape 和显存复用方案规划死。
> beam search 会把 batch 维度实际扩展成 `batch × beam`，
> KV Cache 和各层中间张量的尺寸都跟着变，这个倍数必须构建时已知。
> 运行时只能用小于等于构建值的 beam，想调大就得重新 build engine。
> 这也是 TRT-LLM 灵活性差的一个典型体现。

---

## 五、Triton / kernel 层（会被抽查，别写太满）

### Q15. 你写过 Triton kernel，讲讲 tile 为什么能加速 matmul？

> 因为 naive matmul 的瓶颈是访存不是计算。C 的每个元素要读 A 的一行和 B 的一列，
> 全从 global memory 读，同一行数据被反复读 N 次，算术强度极低。
>
> tile 的做法是把 A、B 切成小块加载到片上 SRAM（shared memory），
> 一个 program 负责算 C 的一个 `BLOCK_M × BLOCK_N` 小块：
> 沿 K 维循环，每次搬一对 `BLOCK_M×BLOCK_K` 和 `BLOCK_K×BLOCK_N` 的块进 SRAM，
> 在片上做完累加再搬下一对。这样每块数据从 global memory 只读一次，
> 在片上被复用 BLOCK 次，算术强度提高了一个数量级。
>
> Triton 的好处是你只写 block 级的逻辑（用 `tl.program_id` 定位自己负责哪一块，
> 用 stride 算地址偏移），线程级的调度和向量化由编译器处理。
>
> ⚠️ 我这块是自学，写过 matmul 和 element-wise 的练习 kernel，
> 没有在生产里写过算子。

**最后那句一定要加。** 写过练习和写过生产算子是两个量级，
不主动划清界限，面试官会按"算子开发"的标准往下问 warp 调度、bank conflict、
double buffering，你会很难受。

---

## 六、快速对照表（面试前扫一眼）

| 概念 | 一句话 |
|---|---|
| **调优三层** | **选型靠实验（要有口径）/ 参数靠显存账 + 瓶颈判断 / 代码靠 profiling** |
| PagedAttention | KV Cache 分页，消碎片 + 可共享 |
| Prefix Caching | 块级链式哈希，命中则跳过 prefill 计算 |
| Continuous Batching | 迭代级组批，完成即出队，避免等最长请求 |
| Chunked Prefill | 切 prefill 与 decode 混批，压 TPOT 尖刺 |
| CUDA Graph | 压 kernel launch 开销，要求 shape 固定 |
| GQA | 减 KV 头数，直接砍 KV Cache 显存 |
| Speculative Decoding | 小模型猜、大模型验，省 decode 步数 |
| TP / PP | 层内切（通信频、要带宽）/ 层间切（有气泡） |
| per-tensor / per-channel | 一个 scale / 每通道一个 scale，抗离群值 |
| weight-only 量化 | decode（memory-bound）受益，大 batch prefill 可能更慢 |
| TTFT / TPOT / ITL | 首字延迟 / 每字均耗 / 字间间隔 |
