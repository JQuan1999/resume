# 05 · 面试前自查清单

> 面试前一晚过一遍。**重点不是复习知识，是排掉简历上的雷。**

---

## 一、简历风险自查：每一句话你都敢被追问吗？

逐条打勾。**打不了勾的，要么补到能答，要么从简历上删掉。**

### 技能栏（最危险的区域）

- [ ] 写了 **"三大框架的部署与调优"** → 能说出**三层调优方法论**吗？
      （选型靠实验但有口径 / 参数靠显存账 + 瓶颈判断 / 代码靠 profiling，见 01-Q0）
      ⚠️ 只会说"哪个好用用哪个"= 当场判定没有方法论。
- [ ] 写了 **CUDA** → 能说清 grid/block/warp、shared memory、coalesced access 吗？
      只会调 API 的话，改成"了解 GPU 编程模型"。
- [ ] 写了 **量化（INT8/FP8/AWQ/GPTQ）** → 能答"精度损失从哪来"和
      "per-tensor vs per-channel"吗？（见 01-Q10）答不了就删或降级为"了解"。
- [ ] 写了 **Triton** → 明确区分「Triton 语言（写 kernel）」和「Triton Inference Server」，
      面试官会混。你会的是前者，而且是练习级别，主动说明。
- [ ] 写了 **PagedAttention / Prefix Caching / Continuous Batching** →
      01 的 Q1/Q2/Q5 是必答题，不能卡。
- [ ] 写了 **张量并行 TP** → 能说清切法和 AllReduce 位置（01-Q8）。
- [ ] 写了 **昇腾** → 能说出你实际做到哪一层（部署？算子？），踩过什么具体的坑。
- [ ] 写了 **coredump / 性能排查** → 02-Q10 的方法论要能脱口而出。

### 项目描述

- [ ] **"节省 200 余张 T4"** → 怎么算的？哪些模型？量化还是调参还是升级各占多少？
- [ ] **"p99 从 200ms 降到 50ms"** → 03-Q2，必被追问，必须有归因。
- [ ] **"支撑 200 余个模型"** → 有哪几类？最大的那个多大？
- [ ] **"排查时间 30 分钟降到 5 分钟"** → 怎么统计的？样本多少？
- [ ] **"D2H 同步次数 O(n) 降到 1"** → 01-Q13，这是你的招牌，要讲得流畅。
- [ ] 每个 **【补】** 都填了真实数字，简历上**没有残留占位符**。

### 边界声明（主动说，别等被问出来）

- [ ] `libfilter` / `DTreeClass` 底层树结构是部门已有基础库，不是我实现的
- [ ] UCS 是公司内部 RPC 框架（类比 brpc），不是我写的
- [ ] nano-vLLM 是开源项目，我做的是精读 + 注释 + 写验证测试，不是我实现的
- [ ] Triton kernel 是练习代码，没有生产经验
- [ ] GR 约束解码方案**未上线**
- [ ] 昇腾我做的是部署适配层，没做算子级优化
- [ ] 量化用的是开源工具链 + 业务数据校准，没自己实现量化算法（01-Q0 / Q10）
- [ ] 框架层的调优以实验对比为主，代码级 profiling 优化只有 GR 这一个案例

---

## 二、必须能脱口而出的 8 个回答

不用背逐字稿，但这 8 个要能不打磕巴地讲 60-90 秒：

1. 自我介绍（04 最后一节）
2. 为什么做推理（04-Q1）
3. PagedAttention + 前缀缓存怎么命中（01-Q1/Q2）
4. Continuous Batching vs 你的动态 batch（01-Q5）—— **你的送分题**
5. GR 的 D2H 瓶颈定位与优化（01-Q13）—— **你的招牌**
6. 为什么 Python 插件用进程不用 pybind11（02-Q6）
7. p99 200→50ms 的归因（03-Q2）
8. 反问 3 个（04 第四节）

> ⭐ 还有一个和 3~5 同等重要：**"你说的调优具体怎么调"（01-Q0 三层方法论）**。
> 它和第 5 题共用 GR 那个案例，但框架不同——**Q0 是方法论，Q13 是案例**，
> 被问 Q0 时先讲三层，再把 Q13 当第三层的例证甩出来。

---

## 三、数字速记卡

面试时要能秒答，别现场算。

| 项 | 数字 |
|---|---|
| 工作年限 | 2024.07 至今，2 年+ |
| 支撑模型数 | 200+ 小模型 |
| 节省显卡 | 200+ 张 T4 |
| 敏感词 QPS / p99 | 单机房 10w QPS，p99 200ms → 50ms |
| 干预时效 | 10 分钟 → 1 分钟以内 |
| 数据通道规模 | 100+ 服务，3000+ 词典，4 机房 2000+ 台机器 |
| 排查提效 | 30 分钟 → 5 分钟以内 |
| GR 词表维度 | 151936 |
| GR beam width | 5（engine 构建期烘焙） |
| nano-vLLM 代码量 | ~1300 行 |
| 诊断 Agent 工具数 | 7 个只读工具 |
| TRT 升级跨度 | 7.x → 10.13，gcc 4.9 → 13.1 |

---

## 四、临场原则

1. **不会就说不会。** 标准句式：
   > "这块我没有深入做过。我的理解是……，不知道对不对，您能展开讲讲吗？"
   > 推理岗面试官对"知道边界"的容忍度很高，对"硬编"的容忍度是零。

2. **先给结论再展开。** 每个技术问题先用一句话回答，再问"需要我展开吗"。
   别一上来讲五分钟，面试官想听的点你可能根本没提到。

3. **主动交代未上线和未做的部分。** 主动说 > 被问出来 > 被发现在遮掩。

4. **被问倒之后不要慌。** 一题答不上来不影响整体评价，
   但因为答不上来而乱了阵脚、后面的题也发挥失常，才是真正致命的。

5. **每答完一个业务题，把话题带回推理。**
   > "这块是我前一年的工作，后来重心转到推理这边了……"

---

## 五、面试后

- 当天记录：被问到的题、答得不好的点、面试官关注的方向
- 答不上来的题当天补，下一场就能用上
- 面完 2-3 家后回头修简历——面试官反复追问的地方，说明简历没写清楚

---

## 六、还没做但建议做的两件事

1. **开 GitHub**，传三样东西：nano-vLLM 注释版 + KV Cache 插桩测试脚本、
   Triton 练习 kernel、双数组 Trie 的实现和原理文档。
   简历顶部挂链接。这是 2 年经验转推理岗**性价比最高的加分项**——
   它把"我自学过"从一句话变成可验证的东西。

2. **把 GR 那个方案的剩余瓶颈真的优化一次。**
   预分配常驻 mask buffer、索引改成常驻显存 tensor，
   这两个改动不大，但如果能让 P99 再降一截，
   这条从"评估后否掉"变成"优化到达标"，价值完全不同。
   而且这是你现在就能做、且能直接写进简历的事。
